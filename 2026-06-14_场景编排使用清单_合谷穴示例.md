# MoneyPrinterTurbo 场景编排使用清单 · 合谷穴示例

> 适用版本：v1.2.9 + WebUI 场景编排补丁（2026-06-14）
> 适用场景：中医穴位科普 / 知识卡片类视频，需要「金句卡 + 实拍/解剖图」混排
> 期望产出：24-30s 短视频，6 张场景卡按顺序播放

---

## 0 · 背景

MoneyPrinterTurbo 原生 WebUI 只有「单一视频源」（Pexels / Pixabay / 本地）一种画面流，**没法在视频中间插入「金句卡 / 警示卡 / 解剖图」**这类静态画面。

本轮加的「🎬 场景编排」面板让用户在表单里**手动排一个时间线**：
- **文字场景**（标题 / 正文 / 颜色 / 字号 → 实时渲染为 1080×1920 PNG）
- **图片场景**（上传 jpg/png）

提交时 WebUI 把场景列表**自动转成 `VideoParams(video_source="local", video_materials=[...])`**，强插在主流程前，**不动 `tm.start` 内部**。完全复用 `video_source="local"` 现成能力（`preprocess_video` 已经把 png 转 mp4 + Ken Burns）。

---

## 1 · 准备阶段（一次性）

1. 浏览器打开 `http://192.168.1.20:8501/`
2. 准备 1-2 张手部 jpg 图（没有可跳过，**只用文字卡也能出片**）
   - 现成的：`/MoneyPrinterTurbo/resource/anatomy/hegu_full.png`（之前合谷穴改造的解剖图）
   - 或者自己截一张手部特写
3. 准备 30s 旁白脚本（合谷穴科普版）：
   ```
   合谷穴是中医四总穴之一，位置在拇指与食指并拢时，
   手背肌肉隆起的最高点。
   历代医家有面口合谷收的说法，
   头面部的疾病比如牙痛、头痛、咽喉肿痛，
   都可以按压合谷穴来缓解。
   需要注意的是，孕妇不宜按压此穴。
   ```

---

## 2 · 主表单填写（左侧 + 中右面板）

4. **左侧 Video Script Settings**
   - Video Subject：`四总穴之合谷穴`
   - Video Script：粘贴上面那段 30s 旁白
5. **中面板 Video Settings**（先不动 Clip Duration，默认 3s/张）
   - Video Source：保持 `Pexels`（场景编排会**自动覆盖**这里，不用手动改）
   - Video Concat Mode：选 `Sequential`（保持场景顺序）
   - Video Ratio：`Portrait 9:16`
   - Clip Duration：`3` 秒
6. **中面板 Audio Settings**
   - TTS Servers：`Azure TTS V1`
   - Speech Synthesis：搜 `zh` 选 `zh-CN-XiaoxiaoNeural-Female`
7. **中面板 Subtitle Settings**
   - Enable Subtitles：✓
   - Font：`MicrosoftYaHeiBold.ttc`
   - 其余默认

---

## 3 · 打开「🎬 场景编排」（关键步骤）

8. 在中面板最下方找到 **「🎬 场景编排 (0)」** 折叠面板，点开
9. **勾选「☑ 启用场景编排（启用后将覆盖 Video Source）」** ← 不勾则白做
10. 点 **「➕ 文字场景」** 5 次、**「➕ 图片场景」** 1 次（按下面 6 张表填）

> 每张卡顶部有 **[N] 文字·xxx** 标题，下方有「标题 / 正文 / 4 个颜色 / 2 个字号」+ 右侧 200×356 缩略图
> **填完字段后，缩略图会自动重渲**（不点保存）

---

## 4 · 6 张场景卡 · 逐张填写

### 场景 1 · 开场金句卡

| 字段 | 值 |
| --- | --- |
| 标题 | `合谷穴` |
| 正文 | `面口合谷收` |
| 背景色 | `#0F2A4A`（深蓝） |
| 标题色 | `#FFD700`（金） |
| 正文字色 | `#FFFFFF`（白） |
| 装饰条色 | `#FF6B6B`（珊瑚红） |
| 标题字号 | `140` |
| 正文字号 | `72` |

**效果预览**：深蓝底 + 金色大字"合谷穴" + 白色"面口合谷收"

---

### 场景 2 · 位置说明（深红 + 白字，警示感强）

| 字段 | 值 |
| --- | --- |
| 标题 | `位置` |
| 正文 | `手背第1、2掌骨间` ↩ `拇指食指并拢` ↩ `肌肉最高点` |
| 背景色 | `#5C0F1A`（酒红） |
| 标题色 | `#FFFFFF` |
| 正文字色 | `#FFFFFF` |
| 装饰条色 | `#FFD700`（金） |
| 标题字号 | `140` |
| 正文字号 | `72` |

**注意**：正文里的 `↩` 表示**按回车换行**（多行字符串）。PIL 内部会按换行符强制断行 + 按像素宽自动再换行。

---

### 场景 3 · 图片场景（解剖图）

1. 点 **「➕ 图片场景」** 加 1 张图片卡
2. 卡片内点 **「上传图片」**，选 `/MoneyPrinterTurbo/resource/anatomy/hegu_full.png`（或你准备的手部 jpg）
3. 上传后右侧自动出现 200×356 预览
4. 不需要调其他字段（图片场景没有颜色/字号）

---

### 场景 4 · 功效列表（清新绿）

| 字段 | 值 |
| --- | --- |
| 标题 | `功效` |
| 正文 | `牙痛  头痛` ↩ `咽喉肿痛  面瘫` |
| 背景色 | `#1A4A2E`（墨绿） |
| 标题色 | `#FFFFFF` |
| 正文字色 | `#FFFFFF` |
| 装饰条色 | `#90EE90`（浅绿） |
| 标题字号 | `140` |
| 正文字号 | `72` |

---

### 场景 5 · 文字卡（继续正文，过渡）

| 字段 | 值 |
| --- | --- |
| 标题 | `按压` |
| 正文 | `拇指指腹按压` ↩ `力度以酸胀为宜` ↩ `每次3-5分钟` |
| 背景色 | `#0F2A4A`（深蓝，呼应开场） |
| 标题色 | `#FFD700` |
| 正文字色 | `#FFFFFF` |
| 装饰条色 | `#FF6B6B` |
| 标题字号 | `140` |
| 正文字号 | `72` |

---

### 场景 6 · 结尾警示（深红警示色）

| 字段 | 值 |
| --- | --- |
| 标题 | `注意` |
| 正文 | `孕妇慎用` ↩ `以免引起子宫收缩` |
| 背景色 | `#3A0F0F`（深红） |
| 标题色 | `#FFFFFF` |
| 正文字色 | `#FFFFFF` |
| 装饰条色 | `#FF4444`（亮红） |
| 标题字号 | `140` |
| 正文字号 | `72` |

---

## 5 · 顺序调整（如果需要）

11. 如果你加卡的顺序与上面不一致，用每张卡片右上角的 **↑ / ↓** 调
12. 想要删除某张卡点 **🗑️**（自动清理磁盘上的产物 PNG）

---

## 6 · 提交生成

13. 滚到最下面，点 **「Generate Video」**（蓝色大按钮）
14. 等待约 1-3 分钟（场景 6 张 × 3s = 18s + TTS 合成）
15. 页面下方会出 **「Video Generation Completed」** + 视频播放器
16. 左侧 sidebar **「📋 历史」** 自动多一条 `2026-06-14 xx:xx · 四总穴之合谷穴 · ✅`

---

## 7 · 验证产物

17. 浏览器里直接看 final 视频（应该按场景 1-2-3-4-5-6 顺序播放）
18. 容器内验证：
    ```bash
    # 看任务目录
    docker exec moneyprinterturbo-api ls /MoneyPrinterTurbo/storage/tasks/$(ls -t /MoneyPrinterTurbo/storage/tasks/ | head -1)/
    # 应该看到：
    #   audio.mp3  combined-1.mp4  final-1.mp4  final-1-overlay.mp4  script.txt  subtitle.srt
    #   scene_*.png  (场景渲染产物)
    ```
19. 抽帧验证（看 5s / 12s / 20s 三个时间点）：
    ```bash
    TASK_DIR=$(ls -td /MoneyPrinterTurbo/storage/tasks/*/ | head -1)
    docker exec moneyprinterturbo-api ffmpeg -ss 5  -i ${TASK_DIR}final-1-overlay.mp4 -vframes 1 -y /tmp/check_5s.jpg
    docker exec moneyprinterturbo-api ffmpeg -ss 12 -i ${TASK_DIR}final-1-overlay.mp4 -vframes 1 -y /tmp/check_12s.jpg
    docker exec moneyprinterturbo-api ffmpeg -ss 20 -i ${TASK_DIR}final-1-overlay.mp4 -vframes 1 -y /tmp/check_20s.jpg
    ```
20. 拷贝到本地预览：
    ```bash
    scp magiccops@192.168.1.20:/MoneyPrinterTurbo/storage/tasks/$(ls -t /home/magiccops/MoneyPrinterTurbo/storage/tasks/ | head -1)/final-1-overlay.mp4 ./
    ```

---

## 8 · 复用 / 变体

| 想要 | 怎么改 |
| --- | --- |
| **换主题**（足三里、内关...） | 改主表单的 Video Subject + 改场景 1-6 的标题/正文；缩略图实时跟着变 |
| **整套配色** | 6 张卡颜色一致 → 用同 4 个 hex；多色 → 关键卡 2 / 6 用对比色（红/绿） |
| **短版 15s** | 删掉场景 4-5，剩 4 张；或把 Clip Duration 改 2s |
| **长版 60s** | 加 3-4 张「分点卡」（如「合谷穴 - 主治 1」「合谷穴 - 主治 2」...） |
| **加解剖角标** | 在 `config.toml` 写 `anatomy_overlay_image = "/MoneyPrinterTurbo/resource/anatomy/hegu_corner.png"`，重启 webui，final 视频右下角自动叠角标（之前 6-8 改造的功能） |
| **复用上一次配置** | 左侧 sidebar「📋 历史」点上一条 → 表单自动回填 → 微调后再生成 |
| **场景编辑留底** | 当前是 session_state，浏览器关掉就丢；想持久化加 config.toml 的 `[scenes]` section |

---

## 9 · 常见问题

| 现象 | 排查 |
| --- | --- |
| 缩略图一直转圈 | 容器内 PIL 字体回退有问题；`docker logs moneyprinterturbo-webui \| tail -30` 看报错 |
| 文字显示方框/乱码 | 容器内缺 CJK 字体；项目自带 `resource/fonts/MicrosoftYaHeiBold.ttc` 应该够，没装就 `docker exec moneyprinterturbo-api fc-list \| grep -i yahei` 查 |
| 文字卡被放大看不清 | `video_clip_duration` 调小（2s）减小 Ken Burns zoom 幅度；或下一版加「文字卡不 zoom」flag |
| 视频顺序乱了 | 检查场景卡片顺序；提交时若用 `Random` concat mode 会打乱；本例已选 Sequential |
| 提交后报"请先添加场景" | 勾了启用但场景列表空；先加场景再勾 |
| 提交后报"Video Source 不是 pexels/pixabay/local" | 说明场景 override 没生效；看 `docker logs`，应能在 `tm.start` 前看到 `scene editor: N materials, forced video_source=local` 日志 |

---

## 10 · 调色板速查（中医 / 知识科普常用）

| 主题 | 背景 | 标题 | 正文 | 装饰 |
| --- | --- | --- | --- | --- |
| 深蓝主调 | `#0F2A4A` | `#FFD700` 金 | `#FFFFFF` | `#FF6B6B` 红 |
| 酒红警示 | `#5C0F1A` | `#FFFFFF` | `#FFFFFF` | `#FFD700` 金 |
| 墨绿功效 | `#1A4A2E` | `#FFFFFF` | `#FFFFFF` | `#90EE90` 浅绿 |
| 深红结尾 | `#3A0F0F` | `#FFFFFF` | `#FFFFFF` | `#FF4444` 亮红 |
| 米白温润 | `#F5E6D3` | `#5C0F1A` 酒红 | `#3A3A3A` 深灰 | `#A0522D` 棕 |
| 玄黑高冷 | `#1A1A1A` | `#00FFC8` 薄荷绿 | `#E0E0E0` 浅灰 | `#00FFC8` |

---

## 11 · 相关改动文件

- `resource/text_card.py` — 文字卡渲染器（参数化版，新增）
- `webui/Main.py` — 场景编排面板 + 提交时 override（修改）

不动：
- `tm.start` / `preprocess_video` / `combine_videos` / FastAPI

完全复用：
- `video_source="local"` 整条管线
- `app/utils/utils.py:storage_dir("local_videos", create=True)` 路径白名单
- `resource/anatomy/make_hegu.py` 的字体回退链思路
